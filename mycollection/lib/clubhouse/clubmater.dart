import 'package:flutter/material.dart';

class ClubHouseApps extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text(
        'Hello',
        style: TextStyle(fontSize: 30.0),
      ),
    );
  }
}
